{% extends "FOSUserBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
    <div class="row">
        <div class="col-lg-5 well">
            <h2><i class="fa fa-github"></i> Login for Github users</h2>
            <br>
            <p>If you have a Github account, use it to connect to Localhook.</p>
            <br><br><br><br>
            <div class="others text-center">
                {% if error is defined and error %}
                    <span>{{ error }}</span>
                {% endif %}
                {% for owner in hwi_oauth_resource_owners() %}
                    {% if owner == 'github' %}
                        <a href="{{ hwi_oauth_login_url(owner) }}" class="btn btn-primary"><i class="fa fa-github"></i> Login with your Github account</a>
                    {% else %}
                        <a href="{{ hwi_oauth_login_url(owner) }}">{{ owner }}</a> <br />
                    {% endif %}
                {% endfor %}
            </div>
            <br>
            <br>
            <br>
        </div>
        <div class="col-lg-5 well col-lg-offset-1">
            <h2 class="text-center"><i class="fa fa-sign-in"></i> Login for other users</h2>
            <p>Login with your email/username and password.</p>

            {% if error %}
                <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            <form class="form" action="{{ path("fos_user_security_check") }}" method="post">
                <input type="hidden" name="_csrf_token" value="{{ csrf_token }}"/>

                <div class="form-group">
                    <label for="username" class="col-lg-2 control-label">{{ 'security.login.username'|trans }}</label>
                    <div class="col-lg-10">
                        <input type="text" class="form-control" id="username" placeholder="Login" name="_username"
                               required="required">
                    </div>
                </div>

                <div class="form-group">
                    <label for="password" class="col-lg-2 control-label">{{ 'security.login.password'|trans }}</label>
                    <div class="col-lg-10">
                        <input type="password" class="form-control" id="password" placeholder="Password"
                               name="_password" required="required">
                        <div class="checkbox">
                            <label>
                                <input type="checkbox" id="remember_me" name="_remember_me" value="on"/>
                                {{ 'security.login.remember_me'|trans }}
                            </label>
                        </div>
                    </div>
                </div>

                <p class="text-center">
                    <button class="btn btn-success" id="_submit" name="_submit">
                        {{ 'security.login.submit'|trans }}
                    </button>
                    <br>
                <a href="{{ path('fos_user_registration_register') }}">Not already registered?</a>
                </p>
            </form>
        </div>
    </div>
{% endblock fos_user_content %}
